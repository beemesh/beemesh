mod generated {
    pub mod generated_health {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/health_generated.rs"
        ));
    }

    pub mod generated_capacity_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/capacity_request_generated.rs"
        ));
    }

    pub mod generated_capacity_reply {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/capacity_reply_generated.rs"
        ));
    }

    pub mod generated_apply_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_request_generated.rs"
        ));
    }

    pub mod generated_apply_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_response_generated.rs"
        ));
    }

    pub mod generated_keyshare_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/keyshare_response_generated.rs"
        ));
    }

    pub mod generated_handshake {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/handshake_generated.rs"
        ));
    }

    pub mod generated_envelope {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/envelope_generated.rs"
        ));
    }
    pub mod generated_keyshare_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/keyshare_request_generated.rs"
        ));
    }
    pub mod generated_key_shares {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/key_shares_generated.rs"
        ));
    }
    pub mod generated_capability_token {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/capability_token_generated.rs"
        ));
    }

    pub mod generated_distribute_shares_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/distribute_shares_request_generated.rs"
        ));
    }

    pub mod generated_distribute_capabilities_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/distribute_capabilities_request_generated.rs"
        ));
    }

    pub mod generated_assign_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/assign_request_generated.rs"
        ));
    }

    pub mod generated_encrypted_manifest {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/encrypted_manifest_generated.rs"
        ));
    }

    pub mod generated_nodes_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/nodes_response_generated.rs"
        ));
    }

    pub mod generated_candidates_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/candidates_response_generated.rs"
        ));
    }

    pub mod generated_task_create_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/task_create_response_generated.rs"
        ));
    }

    pub mod generated_task_status_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/task_status_response_generated.rs"
        ));
    }

    pub mod generated_distribute_shares_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/distribute_shares_response_generated.rs"
        ));
    }

    pub mod generated_distribute_capabilities_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/distribute_capabilities_response_generated.rs"
        ));
    }

    pub mod generated_assign_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/assign_response_generated.rs"
        ));
    }

    pub mod generated_apply_manifest_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_manifest_request_generated.rs"
        ));
    }

    pub mod generated_apply_manifest_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_manifest_response_generated.rs"
        ));
    }

    pub mod generated_apply_keyshares_request {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_keyshares_request_generated.rs"
        ));
    }

    pub mod generated_apply_keyshares_response {
        #![allow(
            dead_code,
            non_camel_case_types,
            non_snake_case,
            unused_imports,
            unused_variables,
            mismatched_lifetime_syntaxes
        )]
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/apply_keyshares_response_generated.rs"
        ));
    }
}

pub mod machine {
    // Avoid glob imports; re-export specific items below.
    pub use crate::generated::generated_capacity_reply::beemesh::machine::{
        finish_capacity_reply_buffer, root_as_capacity_reply, CapacityReply,
    };
    pub use crate::generated::generated_capacity_request::beemesh::machine::{
        finish_capacity_request_buffer, root_as_capacity_request, CapacityRequest,
    };
    pub use crate::generated::generated_health::beemesh::machine::{root_as_health, Health};
    // Re-export Args to allow building nested FB objects in other modules
    pub use crate::generated::generated_apply_request::beemesh::machine::{
        root_as_apply_request, ApplyRequest,
    };
    pub use crate::generated::generated_apply_response::beemesh::machine::{
        root_as_apply_response, ApplyResponse,
    };
    pub use crate::generated::generated_capacity_reply::beemesh::machine::CapacityReplyArgs;
    pub use crate::generated::generated_capacity_request::beemesh::machine::CapacityRequestArgs;
    pub use crate::generated::generated_envelope::beemesh::machine::{
        root_as_envelope, Envelope as FbEnvelope,
    };
    pub use crate::generated::generated_handshake::beemesh::machine::{
        root_as_handshake, Handshake,
    };
    pub use crate::generated::generated_key_shares::beemesh::machine::{
        root_as_key_shares, KeyShares,
    };
    pub use crate::generated::generated_keyshare_request::beemesh::machine::{
        root_as_key_share_request, KeyShareRequest,
    };
    pub use crate::generated::generated_keyshare_response::beemesh::machine::{
        root_as_key_share_response, KeyShareResponse,
    };
    // Also export Args and helper finish function for builders/tests
    pub use crate::generated::generated_envelope::beemesh::machine::{
        finish_envelope_buffer, EnvelopeArgs,
    };

    // CapabilityToken for key share authorization (generated)
    #[allow(
        dead_code,
        non_camel_case_types,
        non_snake_case,
        unused_imports,
        unused_variables,
        mismatched_lifetime_syntaxes
    )]
    pub mod generated_capability_token {
        pub use crate::generated::generated_capability_token::beemesh::machine::*;
    }

    pub use crate::machine::generated_capability_token::root_as_capability_token;
    pub use crate::machine::generated_capability_token::Capability;
    pub use crate::machine::generated_capability_token::CapabilityArgs;
    pub use crate::machine::generated_capability_token::CapabilityToken;
    pub use crate::machine::generated_capability_token::CapabilityTokenArgs;
    pub use crate::machine::generated_capability_token::Caveat;
    pub use crate::machine::generated_capability_token::CaveatArgs;
    pub use crate::machine::generated_capability_token::SignatureEntry;
    pub use crate::machine::generated_capability_token::SignatureEntryArgs;

    // Distribute shares types
    pub use crate::generated::generated_distribute_shares_request::beemesh::machine::{
        root_as_distribute_shares_request, DistributeSharesRequest, DistributeSharesRequestArgs,
        ShareTarget, ShareTargetArgs,
    };

    // Distribute capabilities types
    pub use crate::generated::generated_distribute_capabilities_request::beemesh::machine::{
        root_as_distribute_capabilities_request, CapabilityTarget, CapabilityTargetArgs,
        DistributeCapabilitiesRequest, DistributeCapabilitiesRequestArgs,
    };

    // Assign request types
    pub use crate::generated::generated_assign_request::beemesh::machine::{
        root_as_assign_request, AssignRequest, AssignRequestArgs,
    };

    // EncryptedManifest types
    pub use crate::generated::generated_encrypted_manifest::beemesh::machine::{
        root_as_encrypted_manifest, EncryptedManifest, EncryptedManifestArgs,
    };

    // Request/Response types for REST API
    pub use crate::generated::generated_apply_keyshares_request::beemesh::machine::{
        root_as_apply_key_shares_request, ApplyKeySharesRequest, ApplyKeySharesRequestArgs,
    };
    pub use crate::generated::generated_apply_keyshares_response::beemesh::machine::{
        root_as_apply_key_shares_response, ApplyKeySharesResponse, ApplyKeySharesResponseArgs,
    };
    pub use crate::generated::generated_apply_manifest_request::beemesh::machine::{
        root_as_apply_manifest_request, ApplyManifestRequest, ApplyManifestRequestArgs,
    };
    pub use crate::generated::generated_apply_manifest_response::beemesh::machine::{
        root_as_apply_manifest_response, ApplyManifestResponse, ApplyManifestResponseArgs,
    };
    pub use crate::generated::generated_assign_response::beemesh::machine::{
        root_as_assign_response, AssignResponse, AssignResponseArgs,
    };
    pub use crate::generated::generated_candidates_response::beemesh::machine::{
        root_as_candidates_response, CandidatesResponse, CandidatesResponseArgs,
    };
    pub use crate::generated::generated_distribute_capabilities_response::beemesh::machine::{
        root_as_distribute_capabilities_response, DistributeCapabilitiesResponse,
        DistributeCapabilitiesResponseArgs,
    };
    pub use crate::generated::generated_distribute_shares_response::beemesh::machine::{
        root_as_distribute_shares_response, DistributeSharesResponse, DistributeSharesResponseArgs,
    };
    pub use crate::generated::generated_nodes_response::beemesh::machine::{
        root_as_nodes_response, NodesResponse, NodesResponseArgs,
    };
    pub use crate::generated::generated_task_create_response::beemesh::machine::{
        root_as_task_create_response, TaskCreateResponse, TaskCreateResponseArgs,
    };
    pub use crate::generated::generated_task_status_response::beemesh::machine::{
        root_as_task_status_response, TaskStatusResponse, TaskStatusResponseArgs,
    };

    // AppliedManifest for DHT storage (generated). Wrap include in a module with
    // liberal allow attributes so generated code doesn't emit warnings.
    #[allow(
        dead_code,
        non_camel_case_types,
        non_snake_case,
        unused_imports,
        unused_variables,
        mismatched_lifetime_syntaxes
    )]
    pub mod generated_applied_manifest {
        include!(concat!(
            env!("CARGO_MANIFEST_DIR"),
            "/src/generated/applied_manifest_generated.rs"
        ));
    }

    pub use crate::machine::generated_applied_manifest::beemesh::machine::{
        root_as_applied_manifest, AppliedManifest, AppliedManifestArgs, KeyValue, KeyValueArgs,
        OperationType, SignatureScheme,
    };

    use base64::Engine as _;
    use flatbuffers::FlatBufferBuilder;

    // Manual builder functions for commonly used flatbuffers
    pub fn build_health(ok: bool, status: &str) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let status_off = fbb.create_string(status);
        let mut args: crate::generated::generated_health::beemesh::machine::HealthArgs =
            Default::default();
        args.ok = ok;
        args.status = Some(status_off);
        let off =
            crate::generated::generated_health::beemesh::machine::Health::create(&mut fbb, &args);
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    pub fn build_capacity_request(
        cpu_milli: u32,
        memory_bytes: u64,
        storage_bytes: u64,
        replicas: u32,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let mut args: crate::generated::generated_capacity_request::beemesh::machine::CapacityRequestArgs = Default::default();
        args.cpu_milli = cpu_milli;
        args.memory_bytes = memory_bytes;
        args.storage_bytes = storage_bytes;
        args.replicas = replicas;
        let off =
            crate::generated::generated_capacity_request::beemesh::machine::CapacityRequest::create(
                &mut fbb, &args,
            );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    pub fn build_capacity_reply(
        ok: bool,
        cpu_available_milli: u32,
        memory_available_bytes: u64,
        storage_available_bytes: u64,
        request_id: &str,
        node_id: &str,
        region: &str,
        kem_pubkey: Option<&str>,
        capabilities: &[&str],
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let req_off = fbb.create_string(request_id);
        let node_off = fbb.create_string(node_id);
        let region_off = fbb.create_string(region);
        let kem_off = kem_pubkey.map(|s| fbb.create_string(s));
        let mut caps_vec: Vec<flatbuffers::WIPOffset<&str>> =
            Vec::with_capacity(capabilities.len());
        for &c in capabilities.iter() {
            caps_vec.push(fbb.create_string(c));
        }
        let caps_off = fbb.create_vector(&caps_vec);
        let mut args: crate::generated::generated_capacity_reply::beemesh::machine::CapacityReplyArgs = Default::default();
        args.ok = ok;
        args.cpu_available_milli = cpu_available_milli;
        args.memory_available_bytes = memory_available_bytes;
        args.storage_available_bytes = storage_available_bytes;
        args.request_id = Some(req_off);
        args.node_id = Some(node_off);
        args.region = Some(region_off);
        if let Some(k) = kem_off {
            args.kem_pubkey = Some(k);
        }
        args.capabilities = Some(caps_off);
        let off =
            crate::generated::generated_capacity_reply::beemesh::machine::CapacityReply::create(
                &mut fbb, &args,
            );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    pub fn build_apply_request(
        replicas: u32,
        tenant: &str,
        operation_id: &str,
        manifest_json: &str,
        origin_peer: &str,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let tenant_off = fbb.create_string(tenant);
        let op_off = fbb.create_string(operation_id);
        let manifest_off = fbb.create_string(manifest_json);
        let origin_off = fbb.create_string(origin_peer);
        let mut args: crate::generated::generated_apply_request::beemesh::machine::ApplyRequestArgs = Default::default();
        args.replicas = replicas;
        args.tenant = Some(tenant_off);
        args.operation_id = Some(op_off);
        args.manifest_json = Some(manifest_off);
        args.origin_peer = Some(origin_off);
        let off = crate::generated::generated_apply_request::beemesh::machine::ApplyRequest::create(
            &mut fbb, &args,
        );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    pub fn build_apply_response(ok: bool, operation_id: &str, message: &str) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let op_off = fbb.create_string(operation_id);
        let msg_off = fbb.create_string(message);
        let mut args: crate::generated::generated_apply_response::beemesh::machine::ApplyResponseArgs = Default::default();
        args.ok = ok;
        args.operation_id = Some(op_off);
        args.message = Some(msg_off);
        let off =
            crate::generated::generated_apply_response::beemesh::machine::ApplyResponse::create(
                &mut fbb, &args,
            );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    pub fn build_keyshare_response(ok: bool, operation_id: &str, message: &str) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let op_off = fbb.create_string(operation_id);
        let msg_off = fbb.create_string(message);
        let mut args: crate::generated::generated_keyshare_response::beemesh::machine::KeyShareResponseArgs = Default::default();
        args.ok = ok;
        args.operation_id = Some(op_off);
        args.message = Some(msg_off);
        let off = crate::generated::generated_keyshare_response::beemesh::machine::KeyShareResponse::create(&mut fbb, &args);
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    // KeyShareRequest builder (manifest_id, capability)
    pub fn build_keyshare_request(manifest_id: &str, capability: &str) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let mid = fbb.create_string(manifest_id);
        let cap = fbb.create_string(capability);
        let mut args: crate::generated::generated_keyshare_request::beemesh::machine::KeyShareRequestArgs = Default::default();
        args.manifest_id = Some(mid);
        args.capability = Some(cap);
        let off =
            crate::generated::generated_keyshare_request::beemesh::machine::KeyShareRequest::create(
                &mut fbb, &args,
            );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    // Helpers for Envelope flatbuffer canonicalization and building.
    // New: optional `kem_pub` parameter (Option<&str>) is included so callers may
    // pass a KEM public key (base64-encoded) which will be embedded in the
    // canonical bytes and thus integrity-protected by the envelope signature.
    pub fn build_envelope_canonical(
        payload: &[u8],
        payload_type: &str,
        nonce: &str,
        ts: u64,
        alg: &str,
        kem_pub: Option<&str>,
    ) -> Vec<u8> {
        // Build an Envelope with empty sig/pubkey (canonical bytes to sign)
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let payload_vec = fbb.create_vector(payload);
        let payload_type_off = fbb.create_string(payload_type);
        let nonce_off = fbb.create_string(nonce);
        let alg_off = fbb.create_string(alg);
        let sig_off = fbb.create_string("");
        let pubkey_off = fbb.create_string("");
        // kem_pub is optional; use empty string when not provided so canonicalization
        // remains stable.
        let kem_pub_off = fbb.create_string(kem_pub.unwrap_or(""));
        let peer_id_off = fbb.create_string("");

        let mut args =
            crate::generated::generated_envelope::beemesh::machine::EnvelopeArgs::default();
        args.payload = Some(payload_vec);
        args.payload_type = Some(payload_type_off);
        args.nonce = Some(nonce_off);
        args.ts = ts;
        args.alg = Some(alg_off);
        args.sig = Some(sig_off);
        args.pubkey = Some(pubkey_off);
        args.kem_pubkey = Some(kem_pub_off);
        args.peer_id = Some(peer_id_off);

        let env_off = crate::generated::generated_envelope::beemesh::machine::Envelope::create(
            &mut fbb, &args,
        );
        crate::machine::finish_envelope_buffer(&mut fbb, env_off);
        fbb.finished_data().to_vec()
    }

    pub fn build_envelope_signed(
        payload: &[u8],
        payload_type: &str,
        nonce: &str,
        ts: u64,
        alg: &str,
        sig_prefix: &str,
        sig_b64: &str,
        pubkey_b64: &str,
        kem_pub_b64: Option<&str>,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let payload_vec = fbb.create_vector(payload);
        let payload_type_off = fbb.create_string(payload_type);
        let nonce_off = fbb.create_string(nonce);
        let alg_off = fbb.create_string(alg);
        let sig_full = fbb.create_string(&format!("{}:{}", sig_prefix, sig_b64));
        let pubkey_off = fbb.create_string(pubkey_b64);
        // kem_pub optional - embed base64 string (or empty) in envelope
        let kem_pub_off = fbb.create_string(kem_pub_b64.unwrap_or(""));
        let peer_id_off = fbb.create_string("");

        let mut args =
            crate::generated::generated_envelope::beemesh::machine::EnvelopeArgs::default();
        args.payload = Some(payload_vec);
        args.payload_type = Some(payload_type_off);
        args.nonce = Some(nonce_off);
        args.ts = ts;
        args.alg = Some(alg_off);
        args.sig = Some(sig_full);
        args.pubkey = Some(pubkey_off);
        args.kem_pubkey = Some(kem_pub_off);
        args.peer_id = Some(peer_id_off);

        let env_off = crate::generated::generated_envelope::beemesh::machine::Envelope::create(
            &mut fbb, &args,
        );
        crate::machine::finish_envelope_buffer(&mut fbb, env_off);
        fbb.finished_data().to_vec()
    }

    // Helpers with peer_id support for CLI communication
    pub fn build_envelope_canonical_with_peer(
        payload: &[u8],
        payload_type: &str,
        nonce: &str,
        ts: u64,
        alg: &str,
        peer_id: &str,
        kem_pub: Option<&str>,
    ) -> Vec<u8> {
        // Build an Envelope with empty sig/pubkey (canonical bytes to sign)
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let payload_vec = fbb.create_vector(payload);
        let payload_type_off = fbb.create_string(payload_type);
        let nonce_off = fbb.create_string(nonce);
        let alg_off = fbb.create_string(alg);
        let sig_off = fbb.create_string("");
        let pubkey_off = fbb.create_string("");
        let kem_pub_off = fbb.create_string(kem_pub.unwrap_or(""));
        let peer_id_off = fbb.create_string(peer_id);

        let mut args =
            crate::generated::generated_envelope::beemesh::machine::EnvelopeArgs::default();
        args.payload = Some(payload_vec);
        args.payload_type = Some(payload_type_off);
        args.nonce = Some(nonce_off);
        args.ts = ts;
        args.alg = Some(alg_off);
        args.sig = Some(sig_off);
        args.pubkey = Some(pubkey_off);
        args.kem_pubkey = Some(kem_pub_off);
        args.peer_id = Some(peer_id_off);

        let env_off = crate::generated::generated_envelope::beemesh::machine::Envelope::create(
            &mut fbb, &args,
        );
        crate::machine::finish_envelope_buffer(&mut fbb, env_off);
        fbb.finished_data().to_vec()
    }

    pub fn build_envelope_signed_with_peer(
        payload: &[u8],
        payload_type: &str,
        nonce: &str,
        ts: u64,
        alg: &str,
        sig_prefix: &str,
        sig_b64: &str,
        pubkey_b64: &str,
        peer_id: &str,
        kem_pub_b64: Option<&str>,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let payload_vec = fbb.create_vector(payload);
        let payload_type_off = fbb.create_string(payload_type);
        let nonce_off = fbb.create_string(nonce);
        let alg_off = fbb.create_string(alg);
        let sig_full = fbb.create_string(&format!("{}:{}", sig_prefix, sig_b64));
        let pubkey_off = fbb.create_string(pubkey_b64);
        let kem_pub_off = fbb.create_string(kem_pub_b64.unwrap_or(""));
        let peer_id_off = fbb.create_string(peer_id);

        let mut args =
            crate::generated::generated_envelope::beemesh::machine::EnvelopeArgs::default();
        args.payload = Some(payload_vec);
        args.payload_type = Some(payload_type_off);
        args.nonce = Some(nonce_off);
        args.ts = ts;
        args.alg = Some(alg_off);
        args.sig = Some(sig_full);
        args.pubkey = Some(pubkey_off);
        args.kem_pubkey = Some(kem_pub_off);
        args.peer_id = Some(peer_id_off);

        let env_off = crate::generated::generated_envelope::beemesh::machine::Envelope::create(
            &mut fbb, &args,
        );
        crate::machine::finish_envelope_buffer(&mut fbb, env_off);
        fbb.finished_data().to_vec()
    }

    /// Parse a FlatBuffer Envelope (raw bytes) and return the canonical bytes used for
    /// signature verification together with the decoded signature and pubkey bytes and
    /// the original sig/pub strings. This centralizes the prefix parsing logic (e.g.
    /// handling "ml-dsa-65:BASE64") so callers can use a single trusted implementation.
    pub fn fb_envelope_extract_sig_pub(
        buf: &[u8],
    ) -> anyhow::Result<(Vec<u8>, Vec<u8>, Vec<u8>, String, String)> {
        let fb_env = crate::machine::root_as_envelope(buf)
            .map_err(|e| anyhow::anyhow!("failed to parse envelope flatbuffer: {:?}", e))?;

        let payload_vec = fb_env
            .payload()
            .map(|b| b.iter().collect::<Vec<u8>>())
            .unwrap_or_default();
        let payload_type = fb_env.payload_type().unwrap_or("");
        let nonce = fb_env.nonce().unwrap_or("");
        let ts = fb_env.ts();
        let alg = fb_env.alg().unwrap_or("");
        // Extract optional kem_pub field from envelope (empty when not present).
        // TODO: Fix envelope schema to include kem_pub field
        let kem_pub_field = ""; // fb_env.kem_pub().unwrap_or("");

        let canonical = crate::machine::build_envelope_canonical(
            &payload_vec,
            payload_type,
            nonce,
            ts,
            alg,
            Some(kem_pub_field),
        );

        let sig_field = fb_env.sig().unwrap_or("").to_string();
        let pubkey_field = fb_env.pubkey().unwrap_or("").to_string();

        // Extract base64 portion of signature (after possible prefix)
        let sig_b64 = sig_field
            .splitn(2, ':')
            .nth(if sig_field.contains(':') { 1 } else { 0 })
            .unwrap_or(&sig_field);
        let sig_bytes = base64::engine::general_purpose::STANDARD
            .decode(sig_b64)
            .map_err(|e| anyhow::anyhow!("failed to base64-decode signature: {}", e))?;

        let pub_bytes = base64::engine::general_purpose::STANDARD
            .decode(&pubkey_field)
            .map_err(|e| anyhow::anyhow!("failed to base64-decode pubkey: {}", e))?;

        Ok((canonical, sig_bytes, pub_bytes, sig_field, pubkey_field))
    }

    // Custom handshake builder since it only has string fields
    pub fn build_handshake(
        nonce: u32,
        timestamp: u64,
        protocol_version: &str,
        signature: &str,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(128);
        let proto = fbb.create_string(protocol_version);
        let sig = fbb.create_string(signature);
        let mut args: crate::generated::generated_handshake::beemesh::machine::HandshakeArgs =
            Default::default();
        args.nonce = nonce;
        args.timestamp = timestamp;
        args.protocol_version = Some(proto);
        args.signature = Some(sig);
        let off = crate::generated::generated_handshake::beemesh::machine::Handshake::create(
            &mut fbb, &args,
        );
        fbb.finish(off, None);
        fbb.finished_data().to_vec()
    }

    // Custom builder for AppliedManifest with byte vectors and custom types
    pub fn build_applied_manifest(
        id: &str,
        tenant: &str,
        operation_id: &str,
        origin_peer: &str,
        owner_pubkey: &[u8],
        signature_scheme: SignatureScheme,
        signature: &[u8],
        manifest_json: &str,
        manifest_kind: &str,
        labels: Vec<(String, String)>,
        timestamp: u64,
        operation: OperationType,
        ttl_secs: u32,
        content_hash: &str,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);

        // Create string offsets
        let id_offset = fbb.create_string(id);
        let tenant_offset = fbb.create_string(tenant);
        let operation_id_offset = fbb.create_string(operation_id);
        let origin_peer_offset = fbb.create_string(origin_peer);
        let manifest_json_offset = fbb.create_string(manifest_json);
        let manifest_kind_offset = fbb.create_string(manifest_kind);
        let content_hash_offset = fbb.create_string(content_hash);

        // Create byte vectors
        let owner_pubkey_offset = fbb.create_vector(owner_pubkey);
        let signature_offset = fbb.create_vector(signature);

        // Create labels vector
        let label_offsets: Vec<_> = labels
            .iter()
            .map(|(k, v)| {
                let key_offset = fbb.create_string(k);
                let value_offset = fbb.create_string(v);
                KeyValue::create(
                    &mut fbb,
                    &KeyValueArgs {
                        key: Some(key_offset),
                        value: Some(value_offset),
                    },
                )
            })
            .collect();
        let labels_offset = fbb.create_vector(&label_offsets);

        let args = AppliedManifestArgs {
            id: Some(id_offset),
            tenant: Some(tenant_offset),
            operation_id: Some(operation_id_offset),
            origin_peer: Some(origin_peer_offset),
            owner_pubkey: Some(owner_pubkey_offset),
            signature_scheme,
            signature: Some(signature_offset),
            manifest_json: Some(manifest_json_offset),
            manifest_kind: Some(manifest_kind_offset),
            labels: Some(labels_offset),
            timestamp,
            operation,
            ttl_secs,
            content_hash: Some(content_hash_offset),
        };

        let manifest = AppliedManifest::create(&mut fbb, &args);
        fbb.finish(manifest, None);
        fbb.finished_data().to_vec()
    }

    // Builder function for DistributeSharesRequest
    pub fn build_distribute_shares_request(
        shares_envelope_json: &str,
        targets: &[(String, String)], // (peer_id, payload_json)
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);

        let shares_env_offset = fbb.create_string(shares_envelope_json);

        // Create ShareTarget vector
        let target_offsets: Vec<_> = targets
            .iter()
            .map(|(peer_id, payload_json)| {
                let peer_id_offset = fbb.create_string(peer_id);
                let payload_offset = fbb.create_string(payload_json);
                ShareTarget::create(
                    &mut fbb,
                    &ShareTargetArgs {
                        peer_id: Some(peer_id_offset),
                        payload_json: Some(payload_offset),
                    },
                )
            })
            .collect();
        let targets_offset = fbb.create_vector(&target_offsets);

        let args = DistributeSharesRequestArgs {
            shares_envelope_json: Some(shares_env_offset),
            targets: Some(targets_offset),
        };

        let request = DistributeSharesRequest::create(&mut fbb, &args);
        fbb.finish(request, None);
        fbb.finished_data().to_vec()
    }

    // Builder function for DistributeCapabilitiesRequest
    pub fn build_distribute_capabilities_request(
        targets: &[(String, String)], // (peer_id, payload_json)
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);

        // Create CapabilityTarget vector
        let target_offsets: Vec<_> = targets
            .iter()
            .map(|(peer_id, payload_json)| {
                let peer_id_offset = fbb.create_string(peer_id);
                let payload_offset = fbb.create_string(payload_json);
                CapabilityTarget::create(
                    &mut fbb,
                    &CapabilityTargetArgs {
                        peer_id: Some(peer_id_offset),
                        payload_json: Some(payload_offset),
                    },
                )
            })
            .collect();
        let targets_offset = fbb.create_vector(&target_offsets);

        let args = DistributeCapabilitiesRequestArgs {
            targets: Some(targets_offset),
        };

        let request = DistributeCapabilitiesRequest::create(&mut fbb, &args);
        fbb.finish(request, None);
        fbb.finished_data().to_vec()
    }

    // Builder function for AssignRequest
    pub fn build_assign_request(chosen_peers: &[String]) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        // Create vector of peer ID strings
        let peer_offsets: Vec<_> = chosen_peers
            .iter()
            .map(|peer_id| fbb.create_string(peer_id))
            .collect();
        let peers_offset = fbb.create_vector(&peer_offsets);

        let args = AssignRequestArgs {
            chosen_peers: Some(peers_offset),
        };

        let request = AssignRequest::create(&mut fbb, &args);
        fbb.finish(request, None);
        fbb.finished_data().to_vec()
    }

    // Builder function for EncryptedManifest
    pub fn build_encrypted_manifest(
        nonce: &str,
        payload: &str,
        encryption_algorithm: &str,
        threshold: u32,
        total_shares: u32,
        manifest_type: Option<&str>,
        labels: &[&str],
        encrypted_at: u64,
        content_hash: Option<&str>,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);

        let nonce_offset = fbb.create_string(nonce);
        let payload_offset = fbb.create_string(payload);
        let algorithm_offset = fbb.create_string(encryption_algorithm);

        let manifest_type_offset = manifest_type.map(|s| fbb.create_string(s));
        let content_hash_offset = content_hash.map(|s| fbb.create_string(s));

        // Create labels vector
        let label_offsets: Vec<_> = labels
            .iter()
            .map(|label| fbb.create_string(label))
            .collect();
        let labels_offset = fbb.create_vector(&label_offsets);

        let mut args = crate::generated::generated_encrypted_manifest::beemesh::machine::EncryptedManifestArgs::default();
        args.nonce = Some(nonce_offset);
        args.payload = Some(payload_offset);
        args.encryption_algorithm = Some(algorithm_offset);
        args.threshold = threshold;
        args.total_shares = total_shares;
        args.manifest_type = manifest_type_offset;
        args.labels = Some(labels_offset);
        args.encrypted_at = encrypted_at;
        args.content_hash = content_hash_offset;
        args.version = 1;

        let manifest = crate::generated::generated_encrypted_manifest::beemesh::machine::EncryptedManifest::create(&mut fbb, &args);
        fbb.finish(manifest, None);
        fbb.finished_data().to_vec()
    }

    // Builder for NodesResponse
    pub fn build_nodes_response(peers: &[String]) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);
        let peer_strings: Vec<_> = peers.iter().map(|p| fbb.create_string(p)).collect();
        let peers_vector = fbb.create_vector(&peer_strings);

        let nodes_response =
            crate::generated::generated_nodes_response::beemesh::machine::NodesResponse::create(
                &mut fbb,
                &crate::generated::generated_nodes_response::beemesh::machine::NodesResponseArgs {
                    peers: Some(peers_vector),
                },
            );
        fbb.finish(nodes_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for CandidatesResponse - now includes public keys
    pub fn build_candidates_response_with_keys(
        ok: bool,
        candidates: &[(String, String)],
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);

        // Create CandidateNode objects
        let candidate_offsets: Vec<_> = candidates
            .iter()
            .map(|(peer_id, public_key)| {
                let peer_id_offset = fbb.create_string(peer_id);
                let public_key_offset = fbb.create_string(public_key);
                crate::generated::generated_candidates_response::beemesh::machine::CandidateNode::create(
                    &mut fbb,
                    &crate::generated::generated_candidates_response::beemesh::machine::CandidateNodeArgs {
                        peer_id: Some(peer_id_offset),
                        public_key: Some(public_key_offset),
                    },
                )
            })
            .collect();
        let candidates_vector = fbb.create_vector(&candidate_offsets);

        let candidates_response = crate::generated::generated_candidates_response::beemesh::machine::CandidatesResponse::create(&mut fbb, &crate::generated::generated_candidates_response::beemesh::machine::CandidatesResponseArgs {
            ok,
            candidates: Some(candidates_vector),
        });
        fbb.finish(candidates_response, None);
        fbb.finished_data().to_vec()
    }

    // Backward compatibility wrapper
    pub fn build_candidates_response(ok: bool, responders: &[String]) -> Vec<u8> {
        let candidates: Vec<(String, String)> = responders
            .iter()
            .map(|peer_id| (peer_id.clone(), String::new())) // Empty public key for backward compatibility
            .collect();
        build_candidates_response_with_keys(ok, &candidates)
    }

    // Builder for TaskCreateResponse
    pub fn build_task_create_response(
        ok: bool,
        task_id: &str,
        manifest_id: &str,
        selection_window_ms: u64,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(256);
        let task_id_off = fbb.create_string(task_id);
        let manifest_id_off = fbb.create_string(manifest_id);

        let task_response = crate::generated::generated_task_create_response::beemesh::machine::TaskCreateResponse::create(&mut fbb, &crate::generated::generated_task_create_response::beemesh::machine::TaskCreateResponseArgs {
            ok,
            task_id: Some(task_id_off),
            manifest_id: Some(manifest_id_off),
            selection_window_ms,
        });
        fbb.finish(task_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for TaskStatusResponse
    pub fn build_task_status_response(
        task_id: &str,
        state: &str,
        assigned_peers: &[String],
        shares_distributed: &[String],
        manifest_cid: Option<&str>,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);
        let task_id_off = fbb.create_string(task_id);
        let state_off = fbb.create_string(state);

        let assigned_strings: Vec<_> = assigned_peers
            .iter()
            .map(|p| fbb.create_string(p))
            .collect();
        let assigned_vector = fbb.create_vector(&assigned_strings);

        let distributed_strings: Vec<_> = shares_distributed
            .iter()
            .map(|p| fbb.create_string(p))
            .collect();
        let distributed_vector = fbb.create_vector(&distributed_strings);

        let manifest_cid_off = manifest_cid.map(|c| fbb.create_string(c));

        let status_response = crate::generated::generated_task_status_response::beemesh::machine::TaskStatusResponse::create(&mut fbb, &crate::generated::generated_task_status_response::beemesh::machine::TaskStatusResponseArgs {
            task_id: Some(task_id_off),
            state: Some(state_off),
            assigned_peers: Some(assigned_vector),
            shares_distributed: Some(distributed_vector),
            manifest_cid: manifest_cid_off,
        });
        fbb.finish(status_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for DistributeSharesResponse
    pub fn build_distribute_shares_response(ok: bool, results: &[(String, String)]) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        let _result_strings: Vec<_> = results
            .iter()
            .map(|(peer, result)| {
                let peer_off = fbb.create_string(peer);
                let result_off = fbb.create_string(result);
                (peer_off, result_off)
            })
            .collect();

        // For simplicity, we'll encode results as a JSON string for now
        let results_json = serde_json::to_string(
            &results
                .iter()
                .map(|(k, v)| (k.as_str(), v.as_str()))
                .collect::<std::collections::HashMap<_, _>>(),
        )
        .unwrap_or_default();
        let results_off = fbb.create_string(&results_json);

        let shares_response = crate::generated::generated_distribute_shares_response::beemesh::machine::DistributeSharesResponse::create(&mut fbb, &crate::generated::generated_distribute_shares_response::beemesh::machine::DistributeSharesResponseArgs {
            ok,
            results_json: Some(results_off),
        });
        fbb.finish(shares_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for DistributeCapabilitiesResponse
    pub fn build_distribute_capabilities_response(
        ok: bool,
        results: &[(String, String)],
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        let results_json = serde_json::to_string(
            &results
                .iter()
                .map(|(k, v)| (k.as_str(), v.as_str()))
                .collect::<std::collections::HashMap<_, _>>(),
        )
        .unwrap_or_default();
        let results_off = fbb.create_string(&results_json);

        let capabilities_response = crate::generated::generated_distribute_capabilities_response::beemesh::machine::DistributeCapabilitiesResponse::create(&mut fbb, &crate::generated::generated_distribute_capabilities_response::beemesh::machine::DistributeCapabilitiesResponseArgs {
            ok,
            results_json: Some(results_off),
        });
        fbb.finish(capabilities_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for AssignResponse
    pub fn build_assign_response(
        ok: bool,
        task_id: &str,
        assigned_peers: &[String],
        per_peer_results: &[(String, String)],
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);
        let task_id_off = fbb.create_string(task_id);

        let assigned_strings: Vec<_> = assigned_peers
            .iter()
            .map(|p| fbb.create_string(p))
            .collect();
        let assigned_vector = fbb.create_vector(&assigned_strings);

        let per_peer_json = serde_json::to_string(
            &per_peer_results
                .iter()
                .map(|(k, v)| (k.as_str(), v.as_str()))
                .collect::<std::collections::HashMap<_, _>>(),
        )
        .unwrap_or_default();
        let per_peer_off = fbb.create_string(&per_peer_json);

        let assign_response =
            crate::generated::generated_assign_response::beemesh::machine::AssignResponse::create(
                &mut fbb,
                &crate::generated::generated_assign_response::beemesh::machine::AssignResponseArgs {
                    ok,
                    task_id: Some(task_id_off),
                    assigned_peers: Some(assigned_vector),
                    per_peer_results_json: Some(per_peer_off),
                },
            );
        fbb.finish(assign_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for ApplyManifestResponse
    pub fn build_apply_manifest_response(
        ok: bool,
        tenant: &str,
        replicas_requested: u32,
        assigned_peers: &[String],
        per_peer_results: &[(String, String)],
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);
        let tenant_off = fbb.create_string(tenant);

        let assigned_strings: Vec<_> = assigned_peers
            .iter()
            .map(|p| fbb.create_string(p))
            .collect();
        let assigned_vector = fbb.create_vector(&assigned_strings);

        let per_peer_json = serde_json::to_string(
            &per_peer_results
                .iter()
                .map(|(k, v)| (k.as_str(), v.as_str()))
                .collect::<std::collections::HashMap<_, _>>(),
        )
        .unwrap_or_default();
        let per_peer_off = fbb.create_string(&per_peer_json);

        let manifest_response = crate::generated::generated_apply_manifest_response::beemesh::machine::ApplyManifestResponse::create(&mut fbb, &crate::generated::generated_apply_manifest_response::beemesh::machine::ApplyManifestResponseArgs {
            ok,
            tenant: Some(tenant_off),
            replicas_requested,
            assigned_peers: Some(assigned_vector),
            per_peer_results_json: Some(per_peer_off),
        });
        fbb.finish(manifest_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for ApplyKeySharesResponse
    pub fn build_apply_keyshares_response(ok: bool, results: &[(String, String)]) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        let results_json = serde_json::to_string(
            &results
                .iter()
                .map(|(k, v)| (k.as_str(), v.as_str()))
                .collect::<std::collections::HashMap<_, _>>(),
        )
        .unwrap_or_default();
        let results_off = fbb.create_string(&results_json);

        let keyshares_response = crate::generated::generated_apply_keyshares_response::beemesh::machine::ApplyKeySharesResponse::create(&mut fbb, &crate::generated::generated_apply_keyshares_response::beemesh::machine::ApplyKeySharesResponseArgs {
            ok,
            results_json: Some(results_off),
        });
        fbb.finish(keyshares_response, None);
        fbb.finished_data().to_vec()
    }

    // Builder for encrypted envelope (wraps any payload with encryption)
    pub fn build_encrypted_envelope(
        payload: &[u8],
        payload_type: &str,
        recipient_pubkey: &[u8],
        sender_privkey: &[u8],
        sender_pubkey: &str,
    ) -> anyhow::Result<Vec<u8>> {
        // Encrypt the payload for the recipient
        let encrypted_payload = crypto::encrypt_payload_for_recipient(recipient_pubkey, payload)?;

        // Generate nonce and timestamp
        use std::collections::hash_map::DefaultHasher;
        use std::hash::{Hash, Hasher};
        let mut hasher = DefaultHasher::new();
        std::time::SystemTime::now().hash(&mut hasher);
        let nonce = format!("{:x}", hasher.finish());
        let ts = std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .unwrap_or_default()
            .as_secs();

        // Build canonical envelope for signing
        let canonical = crate::machine::build_envelope_canonical(
            &encrypted_payload,
            payload_type,
            &nonce,
            ts,
            "ml-dsa-65",
            None,
        );

        // Decode sender public key from base64 for signing
        let sender_pubkey_bytes = base64::engine::general_purpose::STANDARD
            .decode(sender_pubkey)
            .map_err(|e| anyhow::anyhow!("Failed to decode sender public key: {}", e))?;

        // Sign the canonical envelope and unpack signature/pub tuple
        let (sig_b64, pub_b64) =
            crypto::sign_envelope(sender_privkey, &sender_pubkey_bytes, &canonical)?;

        // Build the final signed envelope with encrypted payload
        let envelope = build_envelope_signed(
            &encrypted_payload,
            payload_type,
            &nonce,
            ts,
            "ml-dsa-65",
            "ml-dsa-65",
            &sig_b64,
            &pub_b64,
            None,
        );

        Ok(envelope)
    }

    // Builder for encrypted envelope with peer_id support (for CLI communication).
    // Includes optional sender KEM public key (base64) so receivers can cache the
    // sender's KEM pub for future encrypted responses.
    pub fn build_encrypted_envelope_with_peer(
        payload: &[u8],
        payload_type: &str,
        recipient_pubkey: &[u8],
        sender_privkey: &[u8],
        sender_pubkey: &str,
        peer_id: &str,
        sender_kem_pub_b64: Option<&str>,
    ) -> anyhow::Result<Vec<u8>> {
        // Encrypt the payload for the recipient
        let encrypted_payload = crypto::encrypt_payload_for_recipient(recipient_pubkey, payload)?;

        // Generate nonce and timestamp
        use std::collections::hash_map::DefaultHasher;
        use std::hash::{Hash, Hasher};
        let mut hasher = DefaultHasher::new();
        std::time::SystemTime::now().hash(&mut hasher);
        let nonce = format!("{:x}", hasher.finish());
        let ts = std::time::SystemTime::now()
            .duration_since(std::time::UNIX_EPOCH)
            .unwrap_or_default()
            .as_secs();

        // Build canonical envelope for signing (with peer_id) and include optional kem_pub
        let canonical = crate::machine::build_envelope_canonical_with_peer(
            &encrypted_payload,
            payload_type,
            &nonce,
            ts,
            "ml-dsa-65",
            peer_id,
            sender_kem_pub_b64,
        );

        // Decode sender public key from base64 for signing
        let sender_pubkey_bytes = base64::engine::general_purpose::STANDARD
            .decode(sender_pubkey)
            .map_err(|e| anyhow::anyhow!("Failed to decode sender public key: {}", e))?;

        // Sign the canonical envelope and unpack signature/pub tuple
        let (sig_b64, pub_b64) =
            crypto::sign_envelope(sender_privkey, &sender_pubkey_bytes, &canonical)?;

        // Build the final signed envelope with encrypted payload (with peer_id and optional kem_pub)
        let envelope = build_envelope_signed_with_peer(
            &encrypted_payload,
            payload_type,
            &nonce,
            ts,
            "ml-dsa-65",
            "ml-dsa-65",
            &sig_b64,
            &pub_b64,
            peer_id,
            sender_kem_pub_b64,
        );

        Ok(envelope)
    }

    // Builder for ApplyManifestRequest
    pub fn build_apply_manifest_request(
        manifest_envelope_json: &str,
        shares_envelope_json: Option<&str>,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);
        let manifest_off = fbb.create_string(manifest_envelope_json);
        let shares_off = shares_envelope_json.map(|s| fbb.create_string(s));

        let request = crate::generated::generated_apply_manifest_request::beemesh::machine::ApplyManifestRequest::create(&mut fbb, &crate::generated::generated_apply_manifest_request::beemesh::machine::ApplyManifestRequestArgs {
            manifest_envelope_json: Some(manifest_off),
            shares_envelope_json: shares_off,
        });
        fbb.finish(request, None);
        fbb.finished_data().to_vec()
    }

    // Builder for ApplyKeySharesRequest
    pub fn build_apply_keyshares_request(
        shares_envelope_json: &str,
        targets_json: &str,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(1024);
        let shares_off = fbb.create_string(shares_envelope_json);
        let targets_off = fbb.create_string(targets_json);

        let request = crate::generated::generated_apply_keyshares_request::beemesh::machine::ApplyKeySharesRequest::create(&mut fbb, &crate::generated::generated_apply_keyshares_request::beemesh::machine::ApplyKeySharesRequestArgs {
            shares_envelope_json: Some(shares_off),
            targets_json: Some(targets_off),
        });
        fbb.finish(request, None);
        fbb.finished_data().to_vec()
    }

    // Builder for KeyShares
    pub fn build_key_shares(
        shares_b64: &[String],
        n: u32,
        k: u32,
        count: u32,
        manifest_id: &str,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        let share_strings: Vec<_> = shares_b64.iter().map(|s| fbb.create_string(s)).collect();
        let shares_vector = fbb.create_vector(&share_strings);
        let manifest_id_off = fbb.create_string(manifest_id);

        let key_shares =
            crate::generated::generated_key_shares::beemesh::machine::KeyShares::create(
                &mut fbb,
                &crate::generated::generated_key_shares::beemesh::machine::KeySharesArgs {
                    shares: Some(shares_vector),
                    n,
                    k,
                    count,
                    manifest_id: Some(manifest_id_off),
                },
            );
        fbb.finish(key_shares, None);
        fbb.finished_data().to_vec()
    }

    // Builder for CapabilityToken
    pub fn build_capability_token(
        manifest_id: &str,
        issuer_peer_id: &str,
        authorized_peer: &str,
        issued_at: u64,
        expires_at: u64,
    ) -> Vec<u8> {
        let mut fbb = FlatBufferBuilder::with_capacity(512);

        // Create the root capability
        let manifest_id_off = fbb.create_string(manifest_id);
        let issuer_peer_id_off = fbb.create_string(issuer_peer_id);
        let capability_type_off = fbb.create_string("KeyShareRequest");

        let capability =
            crate::generated::generated_capability_token::beemesh::machine::Capability::create(
                &mut fbb,
                &crate::generated::generated_capability_token::beemesh::machine::CapabilityArgs {
                    type_: Some(capability_type_off),
                    manifest_id: Some(manifest_id_off),
                    required_quorum: 1,
                    issued_at,
                    expires_at,
                    issuer_peer_id: Some(issuer_peer_id_off),
                },
            );

        // Create caveat for authorized peer
        let condition_type_off = fbb.create_string("authorized_peer");
        let authorized_peer_bytes = authorized_peer.as_bytes();
        let value_vec = fbb.create_vector(authorized_peer_bytes);

        let caveat = crate::generated::generated_capability_token::beemesh::machine::Caveat::create(
            &mut fbb,
            &crate::generated::generated_capability_token::beemesh::machine::CaveatArgs {
                condition_type: Some(condition_type_off),
                value: Some(value_vec),
            },
        );

        let caveats_vec = fbb.create_vector(&[caveat]);

        // For now, create empty signature chain (will be signed by envelope)
        let empty_signatures: Vec<
            flatbuffers::WIPOffset<
                crate::generated::generated_capability_token::beemesh::machine::SignatureEntry,
            >,
        > = Vec::new();
        let empty_signatures = fbb.create_vector(&empty_signatures);

        // Create the capability token
        let capability_token = crate::generated::generated_capability_token::beemesh::machine::CapabilityToken::create(
            &mut fbb,
            &crate::generated::generated_capability_token::beemesh::machine::CapabilityTokenArgs {
                root_capability: Some(capability),
                caveats: Some(caveats_vec),
                signature_chain: Some(empty_signatures),
            },
        );

        fbb.finish(capability_token, None);
        fbb.finished_data().to_vec()
    }
}

pub mod libp2p_constants;

#[cfg(test)]
mod test {
    use crate::machine::{build_health, root_as_health};

    #[test]
    fn flatbuffers_health_roundtrip() {
        let buf = build_health(true, "healthy");
        // parse and verify
        let health = root_as_health(&buf).unwrap();
        assert!(health.ok());
        assert_eq!(health.status().unwrap(), "healthy");
    }
}
