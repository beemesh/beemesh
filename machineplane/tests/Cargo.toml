[package]
name = "integration"
version = "0.1.0"
edition = "2021"
description = "Integration tests for beemesh machine plane"

[dev-dependencies]
machine = { path = "../machine" }
cli = { path = "../cli" }
tokio = { version = "1", features = ["full"] }
reqwest = {version = "0.11", features = ["json"]}
serde_json = "1.0"
futures = "0.3"
log = "0.4"
env_logger = "0.10"